import Page from "../components/Page";
import {Grid} from "@mui/material";

const Home = (props) => {
  return (
      <Page title="Home">
          <Grid container item>
              ceva
          </Grid>
      </Page>
  );
}
export default Home;